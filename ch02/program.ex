defmodule Program do
  # If a function has no arguments, empty braces are **not** needed.
  def run do
    # This function returns the value, if any returned by `IO.puts/1`
    IO.puts("Surely something interesting.")
  end
end
